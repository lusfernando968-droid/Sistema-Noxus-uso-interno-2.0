{
  "buildCommand": "pnpm run build",
  "installCommand": "pnpm install",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/((?!assets|favicon\\.ico|robots\\.txt|placeholder\\.svg).*)", "destination": "/index.html" }
  ]
}